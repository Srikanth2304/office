CREATE TABLE form_draft_data (
    process_id   VARCHAR2(100) NOT NULL,
    step_name    VARCHAR2(100) NOT NULL,
    form_json    CLOB,
    created_at   TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (process_id, step_name)
);
